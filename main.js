const scoreBoard = document.querySelector(".status");
const winnerAlert = () => "The winner is + ${CurrentPlayer} !"
const drawAlert = () => "There is a DRAW!"
const presentPlayerGo = () "${currentPlayer} + its your turn..."
// applying constructor function